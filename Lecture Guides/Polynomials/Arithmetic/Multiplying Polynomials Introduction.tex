\subsection{Multiplying Polynomials: Introduction} 
To multiply two polynomials (with more than one term), we still apply the\newline\makeblank[1in] property, but we apply it \makeblank[1in].
\begin{example}Multiply $(x^2-3x+2)(2x-1)$, justifying each step.
\why{6}
\end{example}
\noindent The result is to multiply every \makeblank[1.25in] in the first polynomial by every \makeblank[1.25in] in the second. In the next two videos, we'll see two ways to arrange this.