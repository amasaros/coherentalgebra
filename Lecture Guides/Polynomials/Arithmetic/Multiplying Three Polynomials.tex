\subsection{Multiplying Three Polynomials} 
If we wish to multiply three or more polynomials, we do it the same we do with numbers: multiply the \makeblank, then multiply\newline\makeblank.
\begin{example}Multiply $(x-4)(x+2)(x-1)$.\lbr
First we multiply \makeblank[1.5in] using the \makeblank.
\begin{lpic}{}{1}
\end{lpic}
Now we must multiply \makeblank[3in].
\begin{lpic}{}{4}
\end{lpic}
\makeblankfill{$(x-4)(x+2)(x-1)={}$ }
\end{example}