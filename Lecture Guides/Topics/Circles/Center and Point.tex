\subsection{Equation of a Circle Given Center and Point} 
We can give the equation of a circle given the center and a point on the circle.
\begin{Example}
Give the equation of the circle with center $(-2,4)$ and passing through $(2,-1)$.
\begin{itemize}
\item $(h,k)=\blankpair$
\item If the circle passes through $(2,-1)$, the radius must be the \makeblank[1in] from \makeblank[1.5in] to $(2,-1)$.
\item $r=\makeblank=\makeblanksmall$
\item Note $r^2=\makeblanksmall$, so we don't need to worry about the \makeblank[1in].
\item The equation is \makeblank
\end{itemize}
\end{Example}
We can also use this to write the equation of a graphed circle if the radius isn't easy to read.
\begin{Example}
Write the equation of the circle graphed below.
\begin{icpic}
\setgraphsquare{4}
\GraphLarge
\fill (1,-1) circle (0.1);
\draw[graphend] (1,-1) circle ({sqrt(5)});
\end{icpic}
\begin{itemize}
\item $(h,k)=\blankpair$
\item \blankpair\ is on the circle
\item $r=\makeblank=\makeblanksmall$
\item So the equation of the circle is \makeblank
\end{itemize}
\end{Example}