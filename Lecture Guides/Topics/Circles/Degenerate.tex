\subsection{Degenerate and Impossible Circles} 
Two things might go wrong when we try to rewrite a circle in center-radius form.
\begin{example}
Rewrite $x^2+y^2-4x+2y+5=0$ in center-radius form.
\begin{lpic}{}{7}
\end{lpic}
In this equation, we have $r^2=\makeblanksmall$. That's not a circle... is it?
\end{example}
This equation is true only if $(x,y)=\blankpair$. The graph of this equation is just \makeblank. We call this a \makeblank[1.5in] circle.
\begin{example}
Rewrite $x^2+y^2-8x+10y+45=0$
\begin{lpic}{}{7}
\end{lpic}
In this equation, we have $r^2=\makeblanksmall$. But $r$ must be a \makeblank[1in].
\end{example}
If $r^2$ is a \makeblank[1in] number, the equation has \makeblank[1in]. We can say there is \makeblank.