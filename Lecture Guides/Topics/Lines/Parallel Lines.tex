\subsection{Parallel Lines} 
\begin{definition}[Parallel Lines]
Two lines are \term{parallel} if they do not intersect.
\end{definition}
In coordinate terms, parallel lines have the same slope.
\begin{example}
Give an equation of the line parallel to the graph of $y=\frac{2}{3}x-5$ through the point $(-4,2)$.
\begin{itemize}
\item The given line has slope \makeblanksmall
\item A line parallel to the given one has slope \makeblanksmall
\item So the line we want (in point-slope form) is \makeblank
\item Rewrite this as \makeblank
\end{itemize}
\end{example}
If a line is horizontal or vertical, parallel lines are of the same type.
\begin{example}
Give an equation of the line parallel to the graph of $y=7$ through $(-2,4)$.
\begin{itemize}
\item The given line is \makeblank[1in]
\item So the desired line is \makeblank[1in]
\item The line we want is \makeblank[1in]
\end{itemize}
\end{example}
What if a line is given in standard form?
\begin{example}
Give an equation of the line parallel to $2x-3y=5$ through $(-2,1)$.
\begin{lpic}{}{9}
\end{lpic}
\end{example}
Given a line in standard form, we can find a parallel line by \makeblank[1.5in] coefficients, then \makeblank[1.5in] to find the constant.