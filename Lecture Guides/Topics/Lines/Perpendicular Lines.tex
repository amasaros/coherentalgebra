\subsection{Perpendicular Lines} 
\begin{definition}[Perpendicular Lines]
Two lines are \term{perpendicular} if they meet at a right angle.
\end{definition}
In coordinate terms, the slopes of parallel lines are \emph{opposite reciprocals}.
\begin{example}
Give an equation of the line perpendicular to the graph of $y=\frac{3}{4}x+2$ through the point $(6,-3)$.
\begin{itemize}
\item The given line has slope \makeblanksmall
\item A line perpendicular to the given one has slope \makeblanksmall
\item So the line we want (in point-slope form) is \makeblank
\item Rewrite this as \makeblank
\end{itemize}
\end{example}
If a line is horizontal, perpendicular lines are vertical. If a line is vertical, perpendicular lines are horizontal.
\begin{example}
Give an equation of the line perpendicular to the graph of $y=3$ through $(5,-2)$.
\begin{itemize}
\item The given line is \makeblank[1in]
\item So the desired line is \makeblank[1in]
\item The line we want is \makeblank[1in]
\end{itemize}
\end{example}
What if a line is given in standard form?
\begin{example}
Give an equation of the line perpendicular to $3x-5y=10$ through $(-2,1)$?
\begin{lpic}{}{9}
\end{lpic}
\end{example}
Given a line in standard form, we can find a perpendicular line by \makeblank[1in] the coefficients and \makeblank, then \makeblank[1.5in] to find the constant.