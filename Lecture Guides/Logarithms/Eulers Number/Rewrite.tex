\subsection{Change of Base for Exponentials} 
Sometimes, for various reasons, we want to rewrite an exponential expression in a different base, especially base $e$.
\begin{Example}
A certain drug has a half-life in the body of 11 hours. A 200mg dose is injected. Write an exponential function $d(t)$ showing the amount of the drug in the patient's system after $t$ hours. Then, rewrite the function in the form $d(t)=d_0 e^{kt}$. Round $k$ to two decimal places.
\begin{itemize}
\item The half-life is \makeblanksmall hours, so our exponent will be \makeblanksmall.
\item So our function is $d(t)=\makeblank$
\end{itemize}
Now, to rewrite base $e$:
\begin{itemize}
\item We can write our current exponential function as $d(t)=\makeblank[1.5in]$
\item In our goal function, we will have $d(t)=\makeblank[1.5in]$
\item So we want to find $k$ so that \makeblank
\end{itemize}
\begin{lpic}{}{3}
\end{lpic}
Our function is $d(t)=\makeblank$
\end{Example}
