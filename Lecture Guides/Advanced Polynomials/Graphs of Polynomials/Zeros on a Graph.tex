\subsection{Zeros on a Graph} 
Consider the graph below of
\[
f(x)=\tfrac{1}{4}\left(x^3-2x^2-11x+12\right)
\]
\begin{center}\begin{tikzpicture}[x=0.5\linespace,y=0.5\linespace]
	\setgraphlarge
	\GraphSmall
	
	\draw[graph,domain=-3.9230:5.0866,samples=100] plot (\x,{0.25*(\x)^3-0.5*(\x)^2-2.75*(\x)+3});

\end{tikzpicture}\end{center}
This graph has $x$-intercepts at \blankpair, \blankpair, and \blankpair. This is where \makeblank.
\begin{itemize}
\item $f(\makeblank[.25in])=\makeblank[3in]$
\item $f(\makeblank[.25in])=\makeblank[3in]$
\item $f(\makeblank[.25in])=\makeblank[3in]$
\end{itemize}
These are the zeros of $\tfrac{1}{4}\left(x^3-2x^2-11x+12\right)$, as we can see if we factor:
\begin{lpic}{}{8}
\end{lpic}
... but what if the roots have multiplcity?